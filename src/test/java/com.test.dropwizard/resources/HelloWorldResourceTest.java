package com.test.dropwizard.resources;

import static org.mockito.Mockito.mock;

public class HelloWorldResourceTest {

    private final static HelloWorldResource helloWorldResource = mock(HelloWorldResource.class);
}
